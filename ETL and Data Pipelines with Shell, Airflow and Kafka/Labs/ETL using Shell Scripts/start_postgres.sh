#!/bin/bash

echo "Exercise 3 - Start the PostgreSQL Database"

echo -e "\nStep 1: From the Skills Network Tools interface"
echo "-> Navigate to 'Databases'"
echo "-> Choose 'PostgreSQL Database Server'"
echo "-> Click 'Start' to initialize the PostgreSQL server (this may take a few minutes)"

echo -e "\nStep 2: Open the PostgreSQL CLI"
echo "-> Click 'PostgreSQL CLI' from the interface once the server is started"

echo -e "\nYou should now see the interactive psql client with the following prompt:"
echo "postgres=#"

echo -e "\nThis indicates that you're connected and ready to execute PostgreSQL commands."
